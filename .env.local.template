# Next.js Environment Variables for Sanity Integration
# Copy this file to .env.local and fill in your values

# Sanity Project Configuration
NEXT_PUBLIC_SANITY_PROJECT_ID=your-project-id-here
NEXT_PUBLIC_SANITY_DATASET=production
NEXT_PUBLIC_SANITY_API_VERSION=2024-01-25

# Sanity API Token (for server-side operations, preview mode, and ISR)
# Get from: https://sanity.io/manage -> API -> Tokens
# Required permissions: Viewer or Editor
SANITY_API_TOKEN=your-api-token-here

# Preview Mode Secret (generate a random string)
# Used to secure preview mode endpoint
SANITY_PREVIEW_SECRET=your-random-secret-here

# Revalidation Secret (generate a random string)
# Used to secure revalidation webhook
SANITY_REVALIDATE_SECRET=your-random-secret-here

# Site URL (for preview mode redirects)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
